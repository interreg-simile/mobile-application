--- # Default general configuration for the API.

app:
  env: development  # Type of environment
  version: 1        # API version
  port: 8000        # API port
  lngs:             # Supported languages
    - it
  defaultLng: it    # Default language
